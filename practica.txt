esto es un ensayo.

******************** este es una actualizacion desde github***************

******* este comando genera una llave ssh en local
ssh-keygen -t rsa -b 4096 -C "tu@email.com"

eval $(ssh-agent -s): verifica si el agente ssh esta actvo en el sistema debera arroja el pid del agent.
ssh-add + direcion id_rsa